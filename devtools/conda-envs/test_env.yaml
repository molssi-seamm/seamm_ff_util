name: test

channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip

  # SEAMM dependencies
  - molsystem
  - packaging
  - seamm-util

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    # Documentation
    - sphinx-copybutton
