name: test

channels:
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - pillow
  - rdkit

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - seamm_util>=2020.9.23

      # Testing
    - codecov
    - yapf
