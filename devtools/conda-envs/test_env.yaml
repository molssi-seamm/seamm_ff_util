name: test
channels:
  - rdkit
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - rdkit

  # Pip-only installs
  - pip:
    - codecov
    - pillow
    - seamm_util
    - packaging
    - pprint

