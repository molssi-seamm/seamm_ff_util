name: test

channels:
  - rdkit
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - pillow

  # SEAMM dependencies
  - rdkit

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - seamm_util>=2020.12.3

    # Testing
    - yapf
