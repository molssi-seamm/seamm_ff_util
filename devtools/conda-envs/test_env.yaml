name: test

channels:
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - pillow
  - rdkit

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - pprint
    - seamm_util>=0.7

      # Testing
    - codecov
    - yapf
