name: test

channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - pillow

  # SEAMM dependencies
  - rdkit

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pygments

  # Pip-only installs
  - pip:
    - seamm_util>=2020.12.2

    # Documentation
    - rinohtype
    - sphinx-rtd-theme
    - pystemmer
