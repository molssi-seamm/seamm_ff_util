name: test

channels:
  - rdkit
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - pillow

  # SEAMM dependencies
  - rdkit

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - seamm_util>=2020.12.2

    # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
